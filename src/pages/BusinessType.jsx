import React from "react";

const BusinessType = () => {
  return (
    <div className="">
      <div>This is the business type page, still a work-in-progress !</div>
      <div>This is the business type page, still a work-in-progress !</div>
      <div>This is the business type page, still a work-in-progress !</div>
      <div>This is the business type page, still a work-in-progress !</div>
    </div>
  );
};

export default BusinessType;
